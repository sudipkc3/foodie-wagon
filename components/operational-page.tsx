"use client"

import { useEffect, useState, type ReactNode } from "react"
import { ArrowRight, Check, Clock3, MapPin, PackageCheck, Plus, Search, ShoppingBag, Truck, X } from "lucide-react"

type Order = { id: string; customer: string; phone: string; cake: string; source: string; type: string; time: string; status: string; amount: string; urgent: boolean }
const seedOrders: Order[] = [
  { id: "#ORD-1025", customer: "Ram Sharma", phone: "+1 555 0148", cake: "Chocolate Truffle · 1 kg", source: "Emergency", type: "Pickup", time: "Today, 3:30 PM", status: "Preparing", amount: "$68", urgent: true },
  { id: "#ORD-1024", customer: "Priya Kapoor", phone: "+1 555 0192", cake: "Vanilla Berry · 2 kg", source: "Website", type: "Delivery", time: "Today, 5:00 PM", status: "Ready", amount: "$124", urgent: false },
  { id: "#ORD-1023", customer: "Daniel Lee", phone: "+1 555 0167", cake: "Red Velvet · 1 kg", source: "Reception", type: "Pickup", time: "Today, 6:15 PM", status: "New", amount: "$72", urgent: false },
  { id: "#ORD-1022", customer: "Anika Patel", phone: "+1 555 0104", cake: "Mango Cream · 2 kg", source: "Website", type: "Delivery", time: "Today, 7:00 PM", status: "Out for delivery", amount: "$138", urgent: false },
]
const statuses = ["New", "Accepted", "Preparing", "Ready", "Out for delivery", "Completed"]

export function OperationalPage({ active, role }: { active: string; role: string }) {
  const [orders, setOrders] = useState<Order[]>(() => {
    if (typeof window === "undefined") return seedOrders
    try { return JSON.parse(window.localStorage.getItem("foodie-wagon-orders") || "null") || seedOrders } catch { return seedOrders }
  })
  const [query, setQuery] = useState("")
  const [statusFilter, setStatusFilter] = useState("All statuses")
  const [sourceFilter, setSourceFilter] = useState("All sources")
  useEffect(() => { window.localStorage.setItem("foodie-wagon-orders", JSON.stringify(orders)) }, [orders])
  const [showCreate, setShowCreate] = useState(false)
  const [selected, setSelected] = useState<Order | null>(null)
  const filtered = orders.filter((order) => `${order.id} ${order.customer} ${order.phone} ${order.cake}`.toLowerCase().includes(query.toLowerCase()) && (statusFilter === "All statuses" || order.status === statusFilter) && (sourceFilter === "All sources" || order.source === sourceFilter))
  const isKitchen = active === "Kitchen", isDelivery = active === "Delivery", isPayments = active === "Payments"
  if (active === "Customers") return <Customers />
  if (active === "Reports") return <Reports />
  if (active === "Activity logs") return <ActivityLogs />
  const title = isKitchen ? "Kitchen orders" : isDelivery ? "Assigned deliveries" : isPayments ? "Payment activity" : active === "Orders" ? "Order management" : "Today's orders"
  const viewOrders = isKitchen ? filtered.filter((o) => ["New", "Accepted", "Preparing", "Ready"].includes(o.status)) : isDelivery ? filtered.filter((o) => o.type === "Delivery") : filtered
  const advance = (order: Order) => { const next = statuses[Math.min(statuses.indexOf(order.status) + 1, statuses.length - 1)]; setOrders((items) => items.map((item) => item.id === order.id ? { ...item, status: next } : item)); setSelected({ ...order, status: next }) }
  return <div className="space-y-6"><div className="grid gap-4 sm:grid-cols-2 xl:grid-cols-4">{(isKitchen ? [["New orders", "6"], ["Preparing", "8"], ["Ready", "4"], ["Urgent", "1"]] : isDelivery ? [["Assigned", "5"], ["Picked up", "2"], ["Out for delivery", "3"], ["Delivered", "18"]] : isPayments ? [["Collected today", "$2,840"], ["Outstanding", "$460"], ["Refunds", "$120"], ["Transactions", "42"]] : [["Today's orders", String(orders.length + 38)], ["Pending", "6"], ["Preparing", "8"], ["Deliveries", "12"]]).map(([label, value]) => <div className="rounded-2xl border border-[#e9e4dc] bg-white p-5" key={label}><div className="flex justify-between"><p className="text-sm text-[#89847c]">{label}</p><ShoppingBag size={17} className="text-[#e39131]"/></div><p className="mt-4 text-2xl font-bold">{value}</p><p className="mt-1 text-xs text-emerald-600">Live update</p></div>)}</div><div className="rounded-2xl border border-[#e9e4dc] bg-white"><div className="flex flex-col justify-between gap-4 border-b border-[#eeeae3] p-5 sm:flex-row sm:items-center"><div><h2 className="font-serif text-xl font-bold">{title}</h2><p className="mt-1 text-xs text-[#938e86]">{isKitchen ? "Prepare every cake on time with clear requirements." : isDelivery ? "Your route and delivery status at a glance." : "Track the complete order workflow."}</p></div><div className="flex gap-2"><div className="relative"><Search size={15} className="absolute left-3 top-1/2 -translate-y-1/2 text-[#aaa59d]"/><input value={query} onChange={(e) => setQuery(e.target.value)} className="w-44 rounded-lg border border-[#e5e1da] py-2 pl-8 text-xs" placeholder="Search ID, name, phone"/></div><select value={statusFilter} onChange={(e) => setStatusFilter(e.target.value)} className="rounded-lg border border-[#e5e1da] bg-white px-2 py-2 text-xs"><option>All statuses</option>{statuses.map((status) => <option key={status}>{status}</option>)}</select><select value={sourceFilter} onChange={(e) => setSourceFilter(e.target.value)} className="rounded-lg border border-[#e5e1da] bg-white px-2 py-2 text-xs"><option>All sources</option>{["Website", "Walk-in", "Reception", "Emergency", "Phone"].map((source) => <option key={source}>{source}</option>)}</select>{!isKitchen && !isDelivery && !isPayments && <button onClick={() => setShowCreate(true)} className="flex items-center gap-1 rounded-lg bg-[#ee9633] px-3 text-xs font-bold text-white"><Plus size={14}/> New order</button>}</div></div><div className="overflow-x-auto"><table className="w-full min-w-[800px] text-left text-sm"><thead className="bg-[#fcfbf9] text-[10px] uppercase tracking-[.15em] text-[#aaa59c]"><tr><th className="px-5 py-3">Order</th><th className="px-5 py-3">Customer</th><th className="px-5 py-3">Cake</th><th className="px-5 py-3">Fulfillment</th><th className="px-5 py-3">Status</th><th className="px-5 py-3">Action</th></tr></thead><tbody>{viewOrders.map((order) => <tr key={order.id} className="border-t border-[#f0ede8]"><td className="px-5 py-4"><button onClick={() => setSelected(order)} className="font-bold text-[#db8427] hover:underline">{order.id}</button>{order.urgent && <span className="ml-2 rounded bg-red-50 px-1.5 py-1 text-[9px] font-bold text-red-600">URGENT</span>}</td><td className="px-5 py-4"><p className="font-semibold">{order.customer}</p><p className="text-xs text-[#99938a]">{order.source}</p></td><td className="px-5 py-4 text-[#6f6a62]">{order.cake}</td><td className="px-5 py-4"><span className="flex items-center gap-1 text-xs"><MapPin size={13} className="text-[#e39131]"/>{order.type} · {order.time}</span></td><td className="px-5 py-4"><span className="rounded-full bg-[#fff3df] px-2.5 py-1 text-xs font-semibold text-[#bd6c18]">{order.status}</span></td><td className="px-5 py-4"><button onClick={() => advance(order)} className="text-xs font-bold text-[#dc8228]">{order.status === "Completed" ? "View" : isDelivery ? "Update" : "Advance"} <ArrowRight size={13} className="ml-1 inline"/></button></td></tr>)}</tbody></table></div></div>{showCreate && <CreateOrder onClose={() => setShowCreate(false)} onCreate={(order) => { setOrders((items) => [order, ...items]); setShowCreate(false) }} />}{selected && <OrderDetail order={selected} role={role} onClose={() => setSelected(null)} onAdvance={() => advance(selected)} />}</div>
}

function CreateOrder({ onClose, onCreate }: { onClose: () => void; onCreate: (order: Order) => void }) { const [name, setName] = useState(""); const [cake, setCake] = useState("Chocolate Truffle · 1 kg"); const [type, setType] = useState("Pickup"); const [source, setSource] = useState("Reception"); return <Modal title="Create new order" onClose={onClose}><p className="text-xs text-[#8f8981]">Create a walk-in, phone or emergency order.</p><div className="mt-5 grid gap-4 sm:grid-cols-2"><label className="text-sm font-medium sm:col-span-2">Customer name<input value={name} onChange={(e) => setName(e.target.value)} className="mt-2 w-full rounded-lg border border-[#e4dfd7] px-3 py-2.5 text-sm" placeholder="e.g. Ram Sharma"/></label><label className="text-sm font-medium">Cake<select value={cake} onChange={(e) => setCake(e.target.value)} className="mt-2 w-full rounded-lg border border-[#e4dfd7] bg-white px-3 py-2.5 text-sm"><option>Chocolate Truffle · 1 kg</option><option>Vanilla Berry · 2 kg</option><option>Red Velvet · 1 kg</option></select></label><label className="text-sm font-medium">Source<select value={source} onChange={(e) => setSource(e.target.value)} className="mt-2 w-full rounded-lg border border-[#e4dfd7] bg-white px-3 py-2.5 text-sm"><option>Reception</option><option>Walk-in</option><option>Emergency</option><option>Phone</option></select></label><label className="text-sm font-medium">Fulfillment<select value={type} onChange={(e) => setType(e.target.value)} className="mt-2 w-full rounded-lg border border-[#e4dfd7] bg-white px-3 py-2.5 text-sm"><option>Pickup</option><option>Delivery</option></select></label><label className="text-sm font-medium">Required time<input className="mt-2 w-full rounded-lg border border-[#e4dfd7] px-3 py-2.5 text-sm" defaultValue="Today, 6:30 PM"/></label></div><div className="mt-6 flex justify-end gap-3"><button onClick={onClose} className="rounded-lg px-4 py-2 text-sm font-semibold text-[#7a756d]">Cancel</button><button disabled={!name.trim()} onClick={() => onCreate({ id: `#ORD-${1026 + Math.floor(Math.random() * 20)}`, customer: name, phone: "+1 555 0188", cake, source, type, time: "Today, 6:30 PM", status: "New", amount: "$84", urgent: source === "Emergency" })} className="rounded-lg bg-[#ee9633] px-4 py-2 text-sm font-bold text-white disabled:opacity-40">Create order</button></div></Modal> }
function OrderDetail({ order, role, onClose, onAdvance }: { order: Order; role: string; onClose: () => void; onAdvance: () => void }) { return <Modal title={`Order ${order.id}`} onClose={onClose}><div className="flex items-center justify-between rounded-xl bg-[#fff5e7] p-4"><div><p className="font-semibold">{order.customer}</p><p className="text-xs text-[#8f8981]">{order.phone} · {order.source}</p></div><span className="rounded-full bg-white px-3 py-1 text-xs font-bold text-[#bd6c18]">{order.status}</span></div><div className="mt-5 grid gap-4 sm:grid-cols-2"><Info label="Cake details" value={order.cake}/><Info label="Fulfillment" value={`${order.type} · ${order.time}`}/><Info label="Payment" value={`${order.amount} · Paid`}/><Info label="Assigned staff" value={role === "Chef" ? "Ethan Brooks" : "Olivia Martin"}/></div><div className="mt-5 border-t border-[#eeeae3] pt-4"><p className="text-xs font-bold uppercase tracking-wider text-[#a19b92]">Order timeline</p><div className="mt-3 space-y-3">{["Order created · 10:42 AM", "Accepted by reception · 10:45 AM", `${order.status} · now`].map((event) => <div key={event} className="flex items-center gap-3 text-xs text-[#6e6961]"><Check size={14} className="text-emerald-500"/>{event}</div>)}</div></div><button onClick={onAdvance} className="mt-6 w-full rounded-lg bg-[#ee9633] py-3 text-sm font-bold text-white">{order.status === "Completed" ? "Close" : `Mark ${statuses[Math.min(statuses.indexOf(order.status) + 1, statuses.length - 1)]}`}</button></Modal> }
function Info({ label, value }: { label: string; value: string }) { return <div className="rounded-lg border border-[#eeeae3] p-3"><p className="text-[10px] font-bold uppercase tracking-wider text-[#aaa59d]">{label}</p><p className="mt-1 text-sm font-semibold">{value}</p></div> }
function Modal({ title, onClose, children }: { title: string; onClose: () => void; children: ReactNode }) { return <div className="fixed inset-0 z-50 grid place-items-center bg-[#22221f]/50 p-5"><div className="max-h-[90vh] w-full max-w-xl overflow-y-auto rounded-2xl bg-white p-6 shadow-2xl"><div className="flex items-center justify-between"><h2 className="font-serif text-2xl font-bold">{title}</h2><button onClick={onClose} className="rounded-lg p-2 text-[#99948c] hover:bg-[#f6f3ee]"><X size={18}/></button></div>{children}</div></div> }
function ActivityLogs() { const events = [["Olivia Martin", "accepted order #ORD-1023", "2 minutes ago", "Reception"], ["Ethan Brooks", "started preparing #ORD-1025", "8 minutes ago", "Kitchen"], ["Noah Williams", "marked #ORD-1022 out for delivery", "18 minutes ago", "Delivery"], ["Mia Rodriguez", "created website order #ORD-1024", "34 minutes ago", "Orders"], ["Sophia Chen", "recorded payment for #ORD-1021", "1 hour ago", "Payments"]]; return <div className="rounded-2xl border border-[#e9e4dc] bg-white p-6"><div className="flex items-start justify-between"><div><h2 className="font-serif text-2xl font-bold">Activity logs</h2><p className="mt-1 text-sm text-[#8b867e]">A complete trail of staff actions across the bakery.</p></div><span className="rounded-full bg-[#fff3e2] px-3 py-1 text-xs font-bold text-[#c9751f]">Live audit</span></div><div className="mt-6 divide-y divide-[#f0ede8]">{events.map(([employee, action, time, area]) => <div key={employee + action} className="flex items-center justify-between gap-4 py-4"><div className="flex items-center gap-3"><div className="grid h-9 w-9 place-items-center rounded-full bg-[#f8e4cc] text-xs font-bold text-[#a76528]">{employee.split(" ").map((part) => part[0]).join("")}</div><div><p className="text-sm"><strong>{employee}</strong> {action}</p><p className="mt-1 text-xs text-[#9a948b]">{time}</p></div></div><span className="hidden rounded-md bg-[#f7f4ef] px-2 py-1 text-[10px] font-bold uppercase tracking-wider text-[#8c867d] sm:block">{area}</span></div>)}</div></div> }
function Customers() { return <div className="rounded-2xl border border-[#e9e4dc] bg-white p-6"><h2 className="font-serif text-2xl font-bold">Customers</h2><p className="mt-1 text-sm text-[#8b867e]">Find customer history and contact details.</p><div className="mt-6 divide-y divide-[#f0ede8]">{["Ram Sharma", "Priya Kapoor", "Daniel Lee"].map((name, index) => <div className="flex items-center justify-between py-4" key={name}><div><p className="text-sm font-semibold">{name}</p><p className="text-xs text-[#969087]">+1 555 01{48 + index * 19} · {index + 2} orders</p></div><button className="text-xs font-bold text-[#dc8228]">View profile</button></div>)}</div></div> }
function Reports() { return <div className="space-y-6"><div className="grid gap-4 sm:grid-cols-3">{[["Sales today", "$2,840"], ["Orders completed", "38"], ["Average order", "$74.70"]].map(([label, value]) => <div className="rounded-2xl border border-[#e9e4dc] bg-white p-5" key={label}><p className="text-sm text-[#89847c]">{label}</p><p className="mt-4 text-2xl font-bold">{value}</p><p className="mt-1 text-xs text-emerald-600">+12.4% this week</p></div>)}</div><div className="rounded-2xl border border-[#e9e4dc] bg-white p-6"><h2 className="font-serif text-xl font-bold">Sales overview</h2><div className="mt-8 flex h-48 items-end gap-3 border-b border-[#eeeae3] px-2">{[42,58,48,72,64,86,78,94,68,82,88,100].map((height, i) => <div className="flex flex-1 flex-col items-center gap-2" key={i}><div className="w-full rounded-t-md bg-[#f1a044]" style={{ height: `${height}%` }}/><span className="text-[10px] text-[#aaa59d]">{i + 1}</span></div>)}</div></div></div> }
export default OperationalPage
