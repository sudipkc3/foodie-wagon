"use client"

import { useState } from "react"
import Link from "next/link"
import { BarChart3, Bell, Check, ClipboardList, FileText, LayoutDashboard, LogOut, PackageCheck, Send, Settings2, ShieldCheck, ShoppingBag, Truck, UserRound, Users, X } from "lucide-react"
import { OperationsDashboard } from "@/components/operations-dashboard"
import type { StaffAccount } from "@/components/staff-dashboard"

export function StaffDashboardShell({ account, onLogout }: { account: StaffAccount; onLogout: () => void }) {
  const [active, setActive] = useState(account.role === "Chef" ? "Kitchen" : account.role === "Rider" ? "Delivery" : account.role === "Reception" ? "Orders" : "Overview")
  const [notificationsOpen, setNotificationsOpen] = useState(false)
  const [profileOpen, setProfileOpen] = useState(false)
  const [notifications, setNotifications] = useState(["New website order #ORD-1026 received", "Ram Sharma's urgent cake is preparing", "Priya Kapoor is ready for pickup"])
  const [name, setName] = useState(account.name)
  const [phone, setPhone] = useState("+49 841 123 456")
  const [saved, setSaved] = useState(false)
  const navigation = account.role === "Chef" ? [["Kitchen", PackageCheck], ["Orders", ClipboardList], ["Activity logs", FileText]] : account.role === "Rider" ? [["Delivery", Truck], ["Activity logs", FileText]] : account.role === "Accountant" ? [["Overview", LayoutDashboard], ["Reports", BarChart3], ["Payments", ClipboardList], ["Activity logs", FileText]] : [["Overview", LayoutDashboard], ["Orders", ClipboardList], ["Customers", Users], ["Kitchen", PackageCheck], ["Delivery", Truck], ["Reports", BarChart3], ["Staff management", Users], ["Permissions", ShieldCheck], ["WhatsApp", Send], ["Products", ShoppingBag], ["Activity logs", FileText]]

  return <main className="min-h-screen bg-[#f7f5f1] text-[#22221f]"><aside className="fixed inset-y-0 left-0 hidden w-[250px] flex-col border-r border-[#e8e4dc] bg-white px-5 py-6 lg:flex"><Link href="/" className="flex items-center gap-3 px-2"><span className="grid h-10 w-10 place-items-center rounded-xl bg-[#f29b38] text-white">b.</span><span><span className="block font-serif text-lg font-bold leading-none">Bloom & Batter</span><span className="mt-1 block text-[10px] font-semibold uppercase tracking-[.18em] text-[#a3a098]">Staff portal</span></span></Link><div className="mt-12"><p className="px-3 text-[10px] font-bold uppercase tracking-[.18em] text-[#a9a49d]">Workspace</p><nav className="mt-3 max-h-[calc(100vh-250px)] space-y-1 overflow-y-auto">{navigation.map(([label, Icon]) => <button key={label as string} onClick={() => setActive(label as string)} className={`flex w-full items-center gap-3 rounded-xl px-3 py-3 text-left text-sm font-medium transition ${active === label ? "bg-[#fff2df] text-[#c76a10]" : "text-[#75716a] hover:bg-[#faf8f4]"}`}><Icon size={17}/>{label as string}</button>)}</nav></div><button onClick={onLogout} className="mt-auto flex items-center gap-2 px-3 text-sm font-bold text-[#84766d]"><LogOut size={16}/> Sign out</button></aside><section className="lg:pl-[250px]"><header className="flex h-[78px] items-center justify-between border-b border-[#e8e4dc] bg-white px-5 sm:px-8"><div><p className="hidden text-sm text-[#99958d] sm:block">Bloom & Batter <span className="mx-2">/</span> <span className="text-[#252521]">{active}</span></p><p className="mt-1 text-xs text-[#9a8d84] sm:hidden">{account.role} workspace</p></div><div className="flex items-center gap-2"><div className="relative"><button onClick={() => setNotificationsOpen(!notificationsOpen)} className="relative rounded-full p-2.5 text-[#76726b] hover:bg-[#faf8f4]" aria-label="Notifications"><Bell size={19}/>{notifications.length > 0 && <span className="absolute right-1 top-1 h-2 w-2 rounded-full bg-[#ef8d32]"/>}</button>{notificationsOpen && <div className="absolute right-0 top-12 z-40 w-80 rounded-xl border border-[#e9e4dc] bg-white p-4 shadow-xl"><div className="flex items-center justify-between"><strong className="text-sm">Notifications</strong><span className="text-[10px] text-[#e39131]">{notifications.length} new</span></div><div className="mt-3 space-y-2">{notifications.length ? notifications.map((notification) => <div key={notification} className="flex items-start justify-between gap-2 rounded-lg bg-[#fff8ee] p-3 text-xs text-[#6f6a62]"><span><span className="mr-2 inline-block h-1.5 w-1.5 rounded-full bg-[#ee9633]"/>{notification}</span><button onClick={() => setNotifications((items) => items.filter((item) => item !== notification))} className="shrink-0 text-[#aaa59d]" aria-label={`Dismiss ${notification}`}><X size={13}/></button></div>) : <p className="py-3 text-center text-xs text-[#99938a]">You&apos;re all caught up.</p>}</div><button onClick={() => setNotifications([])} className="mt-3 text-xs font-bold text-[#db8427]">Mark all as read</button></div>}</div><div className="h-8 w-px bg-[#ebe7e0]"/><button onClick={() => setProfileOpen(true)} className="flex items-center gap-2 rounded-full p-1 hover:bg-[#faf8f4]" aria-label="Open profile settings"><div className="grid h-9 w-9 place-items-center rounded-full bg-[#f6d6ad] text-xs font-bold text-[#9b5e22]">{account.initials}</div><div className="hidden text-left sm:block"><p className="text-sm font-bold">{name}</p><p className="text-xs text-[#9a8d84]">{account.role}</p></div></button></div></header><div className="p-5 sm:p-8"><OperationsDashboard active={active} role={account.role}/></div></section>{profileOpen && <ProfileModal account={account} name={name} phone={phone} setName={setName} setPhone={setPhone} saved={saved} setSaved={setSaved} onClose={() => setProfileOpen(false)} onLogout={onLogout}/>}</main>
}

function ProfileModal({ account, name, phone, setName, setPhone, saved, setSaved, onClose, onLogout }: { account: StaffAccount; name: string; phone: string; setName: (value: string) => void; setPhone: (value: string) => void; saved: boolean; setSaved: (value: boolean) => void; onClose: () => void; onLogout: () => void }) { return <div className="fixed inset-0 z-50 grid place-items-center bg-[#22221f]/50 p-5"><div className="w-full max-w-lg rounded-2xl bg-white p-6 shadow-2xl"><div className="flex items-start justify-between"><div><p className="text-xs font-bold uppercase tracking-[.16em] text-[#e39131]">Account</p><h2 className="mt-1 font-serif text-2xl font-bold">Profile settings</h2><p className="mt-1 text-xs text-[#938e86]">Update your personal information and notification preferences.</p></div><button onClick={onClose} className="rounded-lg p-2 text-[#99948c] hover:bg-[#f6f3ee]" aria-label="Close profile settings"><X size={18}/></button></div><div className="mt-6 flex items-center gap-3 rounded-xl bg-[#fff5e7] p-4"><div className="grid h-12 w-12 place-items-center rounded-full bg-[#f6d6ad] text-sm font-bold text-[#9b5e22]">{account.initials}</div><div><p className="font-semibold">{account.role}</p><p className="text-xs text-[#938e86]">{account.email}</p></div></div><div className="mt-5 grid gap-4 sm:grid-cols-2"><label className="text-sm font-medium">Full name<input value={name} onChange={(event) => setName(event.target.value)} className="mt-2 w-full rounded-lg border border-[#e4dfd7] px-3 py-2.5 text-sm outline-none focus:border-[#ed9635]"/></label><label className="text-sm font-medium">Phone number<input value={phone} onChange={(event) => setPhone(event.target.value)} className="mt-2 w-full rounded-lg border border-[#e4dfd7] px-3 py-2.5 text-sm outline-none focus:border-[#ed9635]"/></label></div><label className="mt-4 flex items-center gap-3 rounded-lg border border-[#eeeae3] p-3 text-xs text-[#6f6a62]"><input type="checkbox" defaultChecked className="h-4 w-4 accent-[#ee9633]"/> Send me shift and order notifications</label>{saved && <p className="mt-3 flex items-center gap-2 text-xs font-semibold text-emerald-600"><Check size={14}/> Profile changes saved for this demo session.</p>}<div className="mt-6 flex justify-between gap-3"><button onClick={onLogout} className="flex items-center gap-2 rounded-lg px-2 py-2 text-xs font-bold text-[#9b6653]"><LogOut size={14}/> Sign out</button><div className="flex gap-3"><button onClick={onClose} className="rounded-lg px-4 py-2 text-sm font-semibold text-[#7a756d]">Cancel</button><button onClick={() => setSaved(true)} className="rounded-lg bg-[#ee9633] px-4 py-2 text-sm font-bold text-white">Save changes</button></div></div></div></div> }
